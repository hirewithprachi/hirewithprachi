// Placeholder for automated email sequences (to be integrated with Mailchimp/ConvertKit)

export function welcomeEmail(email) {
  // TODO: Integrate with Mailchimp/ConvertKit API for welcome email
  // Example: fetch('/api/welcome', { method: 'POST', body: JSON.stringify({ email }) })
}

export function leadNurtureEmail(email) {
  // TODO: Integrate with Mailchimp/ConvertKit API for lead nurturing
  // Example: fetch('/api/lead-nurture', { method: 'POST', body: JSON.stringify({ email }) })
}

export function onboardingEmail(email) {
  // TODO: Integrate with Mailchimp/ConvertKit API for onboarding
  // Example: fetch('/api/onboarding', { method: 'POST', body: JSON.stringify({ email }) })
} 